"""
    - To use Matplotlib, it must first be installed.
    - You may install it by running the following commands: 
    > pip install matplotlib
    in your terminal
    - Usually, when it comes to plotting a graph, pyplot is a crucial submodule that can make this happen.
"""

from matplotlib import pyplot as plt

## From within the module 'matplotlib', import the submodule 'pyplot' and alias it as 'plt'
